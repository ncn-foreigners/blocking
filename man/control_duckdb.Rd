% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{control_duckdb}
\alias{control_duckdb}
\title{Controls for the DuckDB backend}
\usage{
control_duckdb(
  engine = c("vss", "faiss"),
  install_extension = TRUE,
  dbdir = ":memory:",
  vss_M = 16,
  vss_M0 = 32,
  vss_ef_construction = 128,
  vss_ef_search = 64,
  faiss_index = "IDMap,Flat",
  ...
)
}
\arguments{
\item{engine}{DuckDB vector search engine to use:
\verb{\\"vss\\"} for the core HNSW extension or \verb{\\"faiss\\"} for the
community FAISS extension (currently disabled in the blocking backend).}

\item{install_extension}{should the DuckDB extension be installed
before loading it?}

\item{dbdir}{path to the DuckDB database (default \verb{\\":memory:\\"}).}

\item{vss_M}{maximum number of neighbours kept for each vertex in the
HNSW graph built by the \code{vss} extension.}

\item{vss_M0}{number of neighbours kept at the base level of the HNSW graph.}

\item{vss_ef_construction}{number of candidate vertices considered
during HNSW index construction.}

\item{vss_ef_search}{number of candidate vertices considered during
HNSW search.}

\item{faiss_index}{FAISS factory string, e.g. \verb{\\"IDMap,Flat\\"}
or \verb{\\"IDMap,HNSW32\\"}.}

\item{...}{Additional arguments.}
}
\value{
Returns a list with parameters.
}
\description{
Controls for DuckDB-based blocking that uses the \code{vss} or \code{faiss}
extensions for nearest-neighbour search.
The \code{faiss} engine is reserved for future use and currently errors with
a diagnostic message from the backend.
}
