% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_setup.R
\name{duckdb_setup_vss}
\alias{duckdb_setup_vss}
\title{Set Up DuckDB VSS}
\usage{
duckdb_setup_vss(
  dbdir = ":memory:",
  update = TRUE,
  repository = c("core", "core_nightly"),
  force = FALSE,
  check_index_join = TRUE,
  verbose = interactive()
)
}
\arguments{
\item{dbdir}{path to the DuckDB database used for the setup check
(default \verb{":memory:"}).}

\item{update}{should \code{UPDATE EXTENSIONS (vss)} be executed after the
extension is installed or detected?}

\item{repository}{DuckDB extension repository to use when installation
is required. Use \code{"core"} for the stable repository or
\code{"core_nightly"} to try a newer nightly build.}

\item{force}{should installation be forced from the selected
repository?}

\item{check_index_join}{should a small probe query be run to check
whether \code{HNSW_INDEX_JOIN} is available?}

\item{verbose}{should progress information be printed?}
}
\value{
Returns a list describing the installed extension and whether
\code{HNSW_INDEX_JOIN} was detected. The result also includes the DuckDB
version and, when relevant, a diagnostic note about known upstream
regressions.
}
\description{
Install, update and check the DuckDB \code{vss} extension outside
\code{\link{blocking}()}. This helper prepares the extension once and reports
whether the installed build exposes DuckDB's \code{HNSW_INDEX_JOIN}
optimizer path for batched nearest-neighbour joins.
}
